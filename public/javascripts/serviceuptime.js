CloudFlare.define("serviceuptime", ["serviceuptime/config"],
    function(config) {
        window.ServiceUptime = window.ServiceUptime || {};
        /*
        var script = document.createElement('script');
        script.type = 'text/javascript';
        script.async = true;
        script.src = "//www.serviceuptime.com/cf/js/cloudflare_embed.js";

        var head = document.getElementsByTagName('head')[0];
        head.appendChild(script);
        */
    }
);
